# =============================================================================
# interface :: integration tests
# =============================================================================
# Integration tests that span multiple modules go here.
# Per-module unit tests are in libs/<module>/tests/.

# Placeholder — add integration test targets as they are developed.
# add_subdirectory(integration)
